# Day 81: Week 11 Review and Checkpoint

## Topic
Comprehensive review of ERD notation, drawing ERDs, CHECK/UNIQUE/DEFAULT constraints, and self-assessment checkpoint.

## English Explanation
Today is your Week 11 checkpoint. You will review everything from ERD notation (entities, attributes, relationships, cardinality), drawing ERDs from requirements, and all constraint types (CHECK, UNIQUE, DEFAULT). The self-assessment requires you to design a complete 5-table database from written requirements, proving you can apply all Month 3 skills together.

## Khmer Explanation
ថ្ងៃនេះជាចំណុចត្រួតពិនិត្យសប្តាហ៍ទី១១។ អ្នកនឹងពិនិត្យឡើងវិញអំពី ERD notation (entities, attributes, relationships, cardinality), ការគូរ ERD ពី requirements, និង constraints គ្រប់ប្រភេទ (CHECK, UNIQUE, DEFAULT)។ ការវាយតម្លៃខ្លួនឯងតម្រូវឱ្យអ្នករចនា database ពេញលេញដែលមាន 5 tables ពី requirements ជាអក្សរ។

## SQL Example
```sql
-- Review: CREATE TABLE with all constraint types
CREATE TABLE employees (
    employee_id SERIAL PRIMARY KEY,
    email VARCHAR(100) UNIQUE NOT NULL,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    department VARCHAR(30) DEFAULT 'General',
    salary DECIMAL(10,2) CHECK (salary >= 0),
    hire_date DATE DEFAULT CURRENT_DATE,
    status VARCHAR(10) DEFAULT 'active' CHECK (status IN ('active','inactive','suspended'))
);

CREATE TABLE projects (
    project_id SERIAL PRIMARY KEY,
    project_name VARCHAR(100) UNIQUE NOT NULL,
    budget DECIMAL(12,2) CHECK (budget > 0),
    start_date DATE NOT NULL,
    end_date DATE,
    CHECK (end_date IS NULL OR end_date > start_date)
);

CREATE TABLE assignments (
    assignment_id SERIAL PRIMARY KEY,
    employee_id INT REFERENCES employees(employee_id),
    project_id INT REFERENCES projects(project_id),
    role VARCHAR(50) DEFAULT 'member',
    assigned_date DATE DEFAULT CURRENT_DATE,
    UNIQUE (employee_id, project_id)
);
```

## Hands-on Practice
Self-Assessment Challenge: Design a 5-table database for a **Library Management System**.


Requirements: Track books, members, loans, authors, and categories.
- A book has title, ISBN (unique), publication year, and belongs to one category
- A member has name, email (unique), phone, membership date
- A loan tracks which member borrowed which book, borrow date, due date, return date
- An author can write many books; a book can have many authors
- A category has name and description

Design all 5 tables with appropriate constraints (PK, FK, UNIQUE, CHECK, DEFAULT).

## Technical Vocabulary

| Term | Definition | Khmer |
|------|-----------|-------|
| Checkpoint | A review point to assess progress | ចំណុចត្រួតពិនិត្យ |
| Self-assessment | Evaluating your own understanding | ការវាយតម្លៃខ្លួនឯង |
| Composite constraint | A constraint involving multiple columns | កម្រិតចំណងចម្រុះ |
| ERD notation | Symbols used in entity-relationship diagrams | សញ្ញា ERD |
| Cardinality | The number relationship between entities | ទំនាក់ទំនងចំនួន |

## Speaking Practice
Explain to a client: "Before building your system, I review the database design to ensure all rules are enforced correctly. Constraints like UNIQUE prevent duplicate entries, CHECK ensures valid data ranges, and DEFAULT values reduce errors from missing input."

## Reading Practice
Review your ERDs from Days 76-80. Can you identify all constraints in each diagram? Read through the PostgreSQL documentation section on CREATE TABLE constraints.

## Review Task
Answer these 10 constraint-related questions:
1. What is the difference between UNIQUE and PRIMARY KEY?
2. Can a CHECK constraint reference multiple columns?
3. What happens when you INSERT without specifying a column that has DEFAULT?
4. Can a table have multiple UNIQUE constraints?
5. How do you add a CHECK constraint to an existing table?
6. What is a composite UNIQUE constraint?
7. Can DEFAULT values be expressions (like CURRENT_DATE)?
8. What error occurs when violating a CHECK constraint?
9. How do UNIQUE constraints handle NULL values?
10. Can you combine CHECK and DEFAULT on the same column?

## Expected Result
You can design a complete 5-table database with all appropriate constraints, draw its ERD, and explain each design decision confidently.