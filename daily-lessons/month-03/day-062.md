# Day 62: One-to-Many Relationships

## Topic
One-to-Many Relationships in Database Design

## English Explanation
A one-to-many (1:N) relationship means one row in Table A can relate to many rows in Table B, but each row in Table B relates to only one row in Table A. This is the most common relationship type in databases.

For example: one customer can place many orders, but each order belongs to only one customer. One department has many employees, but each employee belongs to one department.

**How to implement:** Place a foreign key in the "many" side table that references the primary key of the "one" side table. No UNIQUE constraint on the FK — that's what allows multiple rows.

## Khmer Explanation (ការពន្យល់ជាភាសាខ្មែរ)
ទំនាក់ទំនងមួយទៅច្រើន (1:N) មានន័យថា ជួរដេកមួយក្នុងតារាង A អាចទាក់ទងទៅជួរដេកច្រើនក្នុងតារាង B។ ប៉ុន្តែជួរដេកនីមួយៗក្នុងតារាង B ទាក់ទងទៅជួរដេកមួយតែប៉ុណ្ណោះក្នុងតារាង A។

ឧទាហរណ៍៖ អតិថិជនម្នាក់អាចបញ្ជាទិញច្រើន ប៉ុន្តែការបញ្ជាទិញនីមួយៗជារបស់អតិថិជនម្នាក់តែប៉ុណ្ណោះ។ នាយកដ្ឋានមួយមានបុគ្គលិកច្រើន ប៉ុន្តែបុគ្គលិកម្នាក់ស្ថិតក្នុងនាយកដ្ឋានមួយតែប៉ុណ្ណោះ។

វិធីអនុវត្ត៖ ដាក់សោបរទេស (FK) នៅក្នុងតារាងផ្នែក "ច្រើន" ដែលយោងទៅសោចម្បង (PK) នៃតារាងផ្នែក "មួយ"។

## SQL Example

```sql
-- The "one" side: customers table
CREATE TABLE customers (
    customer_id INT PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    registered_date DATE DEFAULT (CURRENT_DATE)
);

-- The "many" side: orders table
CREATE TABLE orders (
    order_id INT PRIMARY KEY AUTO_INCREMENT,
    customer_id INT NOT NULL,
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total_amount DECIMAL(10, 2) NOT NULL,
    status VARCHAR(20) DEFAULT 'pending',
    FOREIGN KEY (customer_id) REFERENCES customers(customer_id)
        ON DELETE RESTRICT
        ON UPDATE CASCADE
);
```


**Key difference from 1:1:** There is NO `UNIQUE` constraint on `customer_id` in the orders table. This allows one customer to have many orders.

## Hands-on Practice

Design a 1:N relationship between `departments` and `employees`:

```sql
CREATE TABLE departments (
    department_id INT PRIMARY KEY AUTO_INCREMENT,
    department_name VARCHAR(100) NOT NULL UNIQUE,
    location VARCHAR(100),
    manager_name VARCHAR(100)
);

CREATE TABLE employees (
    employee_id INT PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    department_id INT,
    salary DECIMAL(10, 2),
    hire_date DATE NOT NULL,
    FOREIGN KEY (department_id) REFERENCES departments(department_id)
        ON DELETE SET NULL
);

-- Insert test data
INSERT INTO departments (department_name, location, manager_name)
VALUES ('Engineering', 'Building A', 'Dara Sok'),
       ('Marketing', 'Building B', 'Maly Chea');

INSERT INTO employees (first_name, last_name, department_id, salary, hire_date)
VALUES ('Sokha', 'Phan', 1, 2500.00, '2023-03-01'),
       ('Vanna', 'Kim', 1, 2200.00, '2023-06-15'),
       ('Bopha', 'Lem', 2, 2000.00, '2024-01-10');
```

**Try this:** Query all employees in the Engineering department using a JOIN.


## Technical Vocabulary

| English Term | Khmer Translation | Definition |
|---|---|---|
| One-to-Many | មួយទៅច្រើន | One row relates to multiple rows in another table |
| Foreign Key | សោបរទេស | Column referencing another table's primary key |
| RESTRICT | ហាមឃាត់ | Prevent deletion if related rows exist |
| SET NULL | កំណត់ជា NULL | Set FK to NULL when parent row is deleted |
| Parent Table | តារាងមេ | The "one" side table |
| Child Table | តារាងកូន | The "many" side table |
| Referential Integrity | សុចរិតភាពយោង | Rules ensuring FK values always match valid PKs |

## Speaking Practice

Explain to a client:
> "A one-to-many relationship is the most common pattern in databases. Think of it like a customer who places multiple orders. The customer exists once, but they can have 10, 50, or even 1000 orders linked to them. We store the customer's ID inside each order to create this connection."

## Reading Practice

Read and analyze this query:

```sql
SELECT c.first_name, c.last_name, COUNT(o.order_id) AS total_orders,
       SUM(o.total_amount) AS total_spent
FROM customers c
LEFT JOIN orders o ON c.customer_id = o.customer_id
GROUP BY c.customer_id, c.first_name, c.last_name
ORDER BY total_spent DESC;
```

**Question:** Why use LEFT JOIN instead of INNER JOIN here?
**Answer:** LEFT JOIN includes customers with zero orders. INNER JOIN would exclude them.


## Review Task

1. What is the difference between 1:1 and 1:N relationships?
2. Which table gets the foreign key in a 1:N relationship — the "one" side or "many" side?
3. Create a 1:N relationship between `authors` and `books`
4. What happens if you add UNIQUE to the FK in a 1:N? (It becomes 1:1!)
5. Name three real-world examples of 1:N relationships

## Expected Result

By the end of today, you should be able to:
- Explain the difference between 1:1 and 1:N relationships clearly
- Create two tables with a proper 1:N relationship
- Know that the FK goes on the "many" side without UNIQUE
- Query 1:N data using JOINs with GROUP BY for counting
- Identify 1:N relationships in real-world scenarios
