# Day 84: Designing an E-Commerce Database

## Topic
Complete e-commerce database with customers, products, categories, orders, order_items, payments, and shipping_addresses.

## English Explanation
An e-commerce database is complex because it handles multiple business domains: product catalog management, customer accounts, order processing, payments, and shipping. Key challenges include: products belonging to categories, orders containing multiple items (order_items junction table), tracking order status through a workflow, handling multiple shipping addresses per customer, and recording payment information securely. Business rules like "order total must match sum of items" and "stock cannot go negative" are enforced through constraints.

## Khmer Explanation
Database e-commerce មានភាពស្មុគស្មាញព្រោះវាគ្រប់គ្រងដែន business ច្រើន៖ ការគ្រប់គ្រង catalog ផលិតផល, គណនីអតិថិជន, ការដំណើរការបញ្ជាទិញ, ការទូទាត់ និងការដឹកជញ្ជូន។ បញ្ហាសំខាន់រួមមាន៖ products ស្ថិតក្នុង categories, orders មានច្រើន items, តាមដាន status, addresses ច្រើនក្នុង customer មួយ និង payment ដែលមានសុវត្ថិភាព។

## SQL Example
```sql
-- Categories with hierarchy
CREATE TABLE categories (
    category_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    parent_id INT REFERENCES categories(category_id),
    description TEXT
);

-- Products
CREATE TABLE products (
    product_id SERIAL PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    description TEXT,
    price DECIMAL(10,2) NOT NULL CHECK (price > 0),
    stock_quantity INT DEFAULT 0 CHECK (stock_quantity >= 0),
    category_id INT REFERENCES categories(category_id),
    sku VARCHAR(50) UNIQUE NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Customers
CREATE TABLE customers (
    customer_id SERIAL PRIMARY KEY,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT NOW()
);


-- Shipping Addresses (one customer can have many)
CREATE TABLE shipping_addresses (
    address_id SERIAL PRIMARY KEY,
    customer_id INT REFERENCES customers(customer_id) ON DELETE CASCADE,
    label VARCHAR(50) DEFAULT 'home',
    street VARCHAR(200) NOT NULL,
    city VARCHAR(100) NOT NULL,
    country VARCHAR(50) NOT NULL,
    postal_code VARCHAR(20),
    is_default BOOLEAN DEFAULT FALSE
);

-- Orders
CREATE TABLE orders (
    order_id SERIAL PRIMARY KEY,
    customer_id INT REFERENCES customers(customer_id),
    shipping_address_id INT REFERENCES shipping_addresses(address_id),
    status VARCHAR(20) DEFAULT 'pending'
        CHECK (status IN ('pending','confirmed','shipped','delivered','cancelled')),
    order_date TIMESTAMP DEFAULT NOW(),
    total_amount DECIMAL(10,2) CHECK (total_amount >= 0)
);

-- Order Items (junction between orders and products)
CREATE TABLE order_items (
    item_id SERIAL PRIMARY KEY,
    order_id INT REFERENCES orders(order_id) ON DELETE CASCADE,
    product_id INT REFERENCES products(product_id),
    quantity INT NOT NULL CHECK (quantity > 0),
    unit_price DECIMAL(10,2) NOT NULL CHECK (unit_price > 0),
    subtotal DECIMAL(10,2) GENERATED ALWAYS AS (quantity * unit_price) STORED
);

-- Payments
CREATE TABLE payments (
    payment_id SERIAL PRIMARY KEY,
    order_id INT REFERENCES orders(order_id),
    amount DECIMAL(10,2) NOT NULL CHECK (amount > 0),
    method VARCHAR(30) CHECK (method IN ('credit_card','paypal','bank_transfer','cash')),
    status VARCHAR(20) DEFAULT 'pending'
        CHECK (status IN ('pending','completed','failed','refunded')),
    paid_at TIMESTAMP
);
```

## Hands-on Practice
1. Create all 7 tables from scratch without looking at the example
2. Insert sample data: 3 categories, 5 products, 2 customers, 3 orders with items
3. Write a query to show order details with product names and quantities
4. Write a query to find total revenue per category
5. Write a query to find customers who have never placed an order

## Technical Vocabulary

| Term | Definition | Khmer |
|------|-----------|-------|
| SKU | Stock Keeping Unit - unique product identifier | កូដផលិតផល |
| Order items | Individual products within an order | ធាតុក្នុងការបញ្ជាទិញ |
| Shipping address | Where an order is delivered | អាសយដ្ឋានដឹកជញ្ជូន |
| Stock quantity | Number of items available | បរិមាណស្តុក |
| Payment method | How the customer pays | វិធីទូទាត់ |

## Speaking Practice
Explain to a client: "Your e-commerce database separates orders from order items. Each order can contain multiple products with different quantities. This design lets us track inventory, calculate totals accurately, and handle partial shipments or returns for individual items."

## Reading Practice
Study how Amazon or Shopify structures their product and order data. Read about the challenges of inventory management in databases (race conditions, stock reservations).

## Review Task
Draw a complete ERD for this e-commerce system. Identify all one-to-many and many-to-many relationships. List 3 business rules enforced by constraints.

## Expected Result
You can design a full e-commerce database handling products, orders, payments, and shipping with proper constraints and relationships between all tables.