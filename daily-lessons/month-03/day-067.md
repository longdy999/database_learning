# Day 67: Week 9 Review and Checkpoint

## Topic
Review all relationship types, junction tables, foreign key syntax, and CASCADE behaviors from Week 9.

## English Explanation
This is your weekly checkpoint. You should now understand one-to-one, one-to-many, and many-to-many relationships. You know how to create junction tables, write proper FK constraints, and choose CASCADE options. Today you review everything, identify weak areas, and write relationship designs from memory to prove mastery.

## Khmer Explanation
នេះគឺជា checkpoint ប្រចាំសប្ដាហ៍។ ឥឡូវអ្នកគួរយល់ពីទំនាក់ទំនង one-to-one, one-to-many និង many-to-many។ អ្នកដឹងពីរបៀបបង្កើត junction table, សរសេរ FK constraints និងជ្រើសរើស CASCADE options។ ថ្ងៃនេះអ្នកពិនិត្យមើលអ្វីៗទាំងអស់ កំណត់ចំណុចខ្សោយ និងសរសេរការរចនាទំនាក់ទំនងពីអង្គចងចាំ។

## SQL Example

```sql
-- 1:1 Relationship Review
CREATE TABLE users (
    user_id INT PRIMARY KEY,
    username VARCHAR(50) NOT NULL
);
CREATE TABLE user_profiles (
    profile_id INT PRIMARY KEY,
    user_id INT UNIQUE,
    bio TEXT,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);

-- 1:N Relationship Review
CREATE TABLE authors (
    author_id INT PRIMARY KEY,
    name VARCHAR(100)
);
CREATE TABLE books (
    book_id INT PRIMARY KEY,
    title VARCHAR(200),
    author_id INT,
    FOREIGN KEY (author_id) REFERENCES authors(author_id)
);


-- M:N Relationship Review
CREATE TABLE students (
    student_id INT PRIMARY KEY,
    name VARCHAR(100)
);
CREATE TABLE courses (
    course_id INT PRIMARY KEY,
    title VARCHAR(100)
);
CREATE TABLE enrollments (
    student_id INT,
    course_id INT,
    enrolled_date DATE,
    PRIMARY KEY (student_id, course_id),
    FOREIGN KEY (student_id) REFERENCES students(student_id) ON DELETE CASCADE,
    FOREIGN KEY (course_id) REFERENCES courses(course_id) ON DELETE CASCADE
);
```

## Hands-on Practice
Write 10 relationship designs from memory (no looking at notes):
1. Customer → Orders (1:N)
2. Student ↔ Courses (M:N with junction)
3. Person → Passport (1:1)
4. Teacher → Classes (1:N)
5. Product ↔ Categories (M:N)
6. Employee → Department (N:1)
7. Doctor ↔ Patients (M:N with appointments junction)
8. Blog Post → Comments (1:N with CASCADE DELETE)
9. User → Profile (1:1 with UNIQUE FK)
10. Movie ↔ Actors (M:N with roles junction)

## Technical Vocabulary

| Term | Definition |
|------|-----------|
| One-to-One (1:1) | Each row in A matches exactly one row in B |
| One-to-Many (1:N) | One row in A matches many rows in B |
| Many-to-Many (M:N) | Multiple rows in A relate to multiple rows in B |
| Junction Table | Bridge table that resolves M:N relationships |
| Composite Key | Primary key made from two or more columns |
| UNIQUE Constraint | Ensures a column has no duplicate values |
| Self-Assessment | Evaluating your own understanding honestly |


## Speaking Practice
"This week I learned three types of relationships. One-to-many is the most common — like customers having multiple orders. Many-to-many requires a junction table because SQL cannot directly link two tables with multiple connections on both sides. CASCADE helps maintain data consistency automatically."

## Reading Practice
Review all your SQL files from this week. Read each CREATE TABLE statement and identify: the relationship type, which table is parent, which is child, and what CASCADE option is used. Make corrections to any mistakes you find.

## Review Task
Self-assessment checklist (answer honestly Yes/No):
- [ ] I can identify 1:1, 1:N, and M:N relationships in real scenarios
- [ ] I can write FK syntax from memory without errors
- [ ] I can create junction tables with composite primary keys
- [ ] I understand ON DELETE CASCADE vs SET NULL vs RESTRICT
- [ ] I can explain referential integrity to a non-technical person
- [ ] I can draw a simple ERD showing relationships

## Expected Result
You can write all three relationship types from memory, explain CASCADE options confidently, and identify any weak areas that need extra practice before moving to normalization next week.
