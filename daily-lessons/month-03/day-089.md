# Day 89: Complete Month 3 Review

## Topic
Full review of all Month 3 skills: relationships, normalization, ERD diagrams, constraints, and design patterns.

## English Explanation
Month 3 covered database design from foundations to professional patterns. You learned about entity relationships (one-to-one, one-to-many, many-to-many), normalization forms (1NF through 3NF), ERD diagrams, constraints (NOT NULL, UNIQUE, CHECK, FOREIGN KEY), and design patterns like lookup tables and audit trails. This review brings everything together.

## Khmer Explanation
ខែទី 3 គ្របដណ្តប់ការរចនា database ពីមូលដ្ឋានដល់ patterns វិជ្ជាជីវៈ។ អ្នកបានរៀនអំពីទំនាក់ទំនង entity (one-to-one, one-to-many, many-to-many), normalization forms (1NF ដល់ 3NF), ERD diagrams, constraints (NOT NULL, UNIQUE, CHECK, FOREIGN KEY), និង design patterns ដូចជា lookup tables និង audit trails។ ការពិនិorg នេះបញ្ចូលអ្វីទាំងអស់គ្នា។

## SQL Example
```sql
-- Month 3 comprehensive example: E-commerce design
CREATE TABLE categories (
    category_id SERIAL PRIMARY KEY,
    category_name VARCHAR(50) UNIQUE NOT NULL
);

CREATE TABLE products (
    product_id SERIAL PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    category_id INT REFERENCES categories(category_id),
    price DECIMAL(10,2) CHECK (price > 0),
    stock_quantity INT DEFAULT 0 CHECK (stock_quantity >= 0),
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE customers (
    customer_id SERIAL PRIMARY KEY,
    email VARCHAR(100) UNIQUE NOT NULL,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE orders (
    order_id SERIAL PRIMARY KEY,
    customer_id INT NOT NULL REFERENCES customers(customer_id),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total_amount DECIMAL(10,2)
);

CREATE TABLE order_items (
    order_item_id SERIAL PRIMARY KEY,
    order_id INT NOT NULL REFERENCES orders(order_id),
    product_id INT NOT NULL REFERENCES products(product_id),
    quantity INT NOT NULL CHECK (quantity > 0),
    unit_price DECIMAL(10,2) NOT NULL
);
```


## Hands-on Practice
Write a complete database design from memory for a "School Management System":
- Students, teachers, courses, enrollments, grades
- Apply normalization, proper naming, constraints, and relationships
- Do NOT look at previous notes — test your memory first, then compare

## Technical Vocabulary

| Term | Definition |
|------|-----------|
| Normalization | Organizing data to reduce redundancy |
| First Normal Form | No repeating groups, atomic values |
| Second Normal Form | No partial dependencies on composite keys |
| Third Normal Form | No transitive dependencies |
| ERD | Entity Relationship Diagram showing table connections |
| Cardinality | The number relationship between entities (1:1, 1:M, M:M) |
| Referential Integrity | Foreign keys always point to valid records |
| Junction Table | Table connecting two entities in M:M relationship |

## Speaking Practice
"Over the past month, I have learned to design databases professionally. I understand how to identify entities and their relationships, normalize data to eliminate redundancy, draw ERD diagrams to visualize the structure, apply constraints to protect data integrity, and use design patterns like lookup tables and audit trails for real-world applications."

## Reading Practice
Self-assessment checklist for Month 3 skills:
- [ ] I can identify 1:1, 1:M, and M:M relationships
- [ ] I can normalize a table from unnormalized to 3NF
- [ ] I can draw an ERD diagram for a 5-table system
- [ ] I can apply NOT NULL, UNIQUE, CHECK, and FK constraints
- [ ] I can use lookup tables for status/type values
- [ ] I can add audit fields to track changes
- [ ] I can review a design and list specific problems
- [ ] I can name tables and columns professionally

## Review Task
1. Design a complete 5-table database from scratch without notes
2. Check your design against the self-assessment checklist above
3. List 3 things you are confident about and 2 things you need more practice with

## Expected Result
You have a clear picture of your Month 3 mastery and know exactly what to reinforce before moving to Month 4 advanced SQL topics.