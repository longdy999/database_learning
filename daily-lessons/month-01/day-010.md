# Day 10: CREATE TABLE — Building Your First Table From Scratch

## Topic
CREATE TABLE — understanding the full syntax for building tables with columns, types, and constraints.

## English Explanation
**CREATE TABLE** is the SQL command that builds a new table in your database. You define the table name, each column name, each column's data type, and any constraints (rules). This is the foundation of database design — every database starts with CREATE TABLE.

**Syntax pattern:**
```
CREATE TABLE table_name (
    column_name DATA_TYPE CONSTRAINT,
    column_name DATA_TYPE CONSTRAINT
);
```

## Khmer Explanation (ការពន្យល់ជាភាសាខ្មែរ)
CREATE TABLE គឺជាពាក្យបញ្ជាសម្រាប់បង្កើតតារាងថ្មីក្នុង database។ អ្នកកំណត់ឈ្មោះតារាង ឈ្មោះជួរឈរនីមួយៗ ប្រភេទទិន្នន័យ និងច្បាប់ (constraints)។ រាល់ database ចាប់ផ្តើមពី CREATE TABLE។

## SQL Example
```sql
-- Full CREATE TABLE with multiple columns, types, and constraints
CREATE TABLE employees (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    first_name TEXT NOT NULL,
    last_name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    department TEXT DEFAULT 'General',
    salary REAL NOT NULL,
    hire_date TEXT NOT NULL,
    is_active INTEGER DEFAULT 1
);

INSERT INTO employees (first_name, last_name, email, department, salary, hire_date)
VALUES ('Sokha', 'Chea', 'sokha@company.com', 'Engineering', 1200.00, '2024-01-15');

INSERT INTO employees (first_name, last_name, email, salary, hire_date)
VALUES ('Dara', 'Kim', 'dara@company.com', 950.00, '2024-03-01');

SELECT * FROM employees;
```


## Hands-on Practice (10-15 minutes)
Create these 3 tables from scratch:
```sql
-- Table 1: Products
CREATE TABLE products (
    product_id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    category TEXT NOT NULL,
    price REAL NOT NULL,
    stock INTEGER DEFAULT 0
);

-- Table 2: Orders
CREATE TABLE orders (
    order_id INTEGER PRIMARY KEY AUTOINCREMENT,
    customer_name TEXT NOT NULL,
    order_date TEXT NOT NULL,
    total_amount REAL NOT NULL,
    status TEXT DEFAULT 'pending'
);

-- Table 3: Employees (write this yourself from memory!)
-- Columns: id, full_name, position, monthly_salary, start_date
```
Insert at least 2 rows into each table and verify with SELECT.

## Technical Vocabulary

| Term | Meaning | Khmer |
|------|---------|-------|
| CREATE TABLE | Command to build a new table | បង្កើតតារាង |
| Column definition | Name + type + constraints for one column | និយមន័យជួរឈរ |
| Constraint | Rule applied to a column (NOT NULL, UNIQUE) | កំហិត |
| Schema | The structure/design of your database tables | គ្រោងរចនាសម្ព័ន្ធ |
| DDL | Data Definition Language (CREATE, ALTER, DROP) | ភាសាកំណត់ទិន្នន័យ |

## Speaking Practice
"I designed the employees table with eight columns. Each column has a specific data type and constraints. For example, email is unique so no two employees can share the same address. The department column defaults to General if not specified."

## Reading Practice
Read your 3 CREATE TABLE statements carefully. For each column, identify: (1) the column name, (2) the data type, (3) any constraints. Practice reading SQL aloud as if explaining the table design to a non-technical client.

## Review Task
- Write a CREATE TABLE statement from memory (no looking!)
- Name 4 common constraints used in CREATE TABLE
- What is the difference between DEFAULT and NOT NULL?

## Expected Result for Today
You can write CREATE TABLE from memory with proper columns, data types, and constraints. You understand that table design is the first step in any database project.