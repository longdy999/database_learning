# Day 14: Week 2 Review and Checkpoint

## Topic
Week 2 Review: Consolidate your knowledge of data types, primary keys, CREATE TABLE, and constraints (NOT NULL, UNIQUE, DEFAULT, CHECK).

## English Explanation
This is your **Week 2 checkpoint**. Before moving forward, you must be confident with:
- All common data types (INTEGER, TEXT, REAL, BLOB)
- PRIMARY KEY and AUTOINCREMENT
- CREATE TABLE syntax from memory
- NOT NULL, UNIQUE, DEFAULT, and CHECK constraints
- Designing tables with appropriate columns and rules

Today you will create a complete mini database from memory without looking at notes. This tests whether you truly learned or just copied.

## Khmer Explanation (ការពន្យល់ជាភាសាខ្មែរ)
ថ្ងៃពិនិត្យឡើងវិញសប្តាហ៍ទី ២

## SQL Example
```sql
-- CHALLENGE: Create a complete online store database from memory
-- You need: customers, products, orders tables
-- Use all constraint types you have learned

-- Table 1: Customers
CREATE TABLE customers (
    customer_id INTEGER PRIMARY KEY AUTOINCREMENT,
    full_name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    phone TEXT UNIQUE,
    city TEXT NOT NULL DEFAULT 'Phnom Penh',
    joined_date TEXT NOT NULL DEFAULT (date('now')),
    is_active INTEGER NOT NULL DEFAULT 1
);

-- Table 2: Products
CREATE TABLE products (
    product_id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    category TEXT NOT NULL,
    price REAL NOT NULL CHECK(price > 0),
    stock INTEGER NOT NULL DEFAULT 0 CHECK(stock >= 0),
    description TEXT DEFAULT 'No description'
);

-- Table 3: Orders
CREATE TABLE orders (
    order_id INTEGER PRIMARY KEY AUTOINCREMENT,
    customer_id INTEGER NOT NULL,
    product_id INTEGER NOT NULL,
    quantity INTEGER NOT NULL DEFAULT 1 CHECK(quantity > 0),
    total_price REAL NOT NULL CHECK(total_price > 0),
    order_date TEXT NOT NULL DEFAULT (date('now')),
    status TEXT NOT NULL DEFAULT 'pending'
        CHECK(status IN ('pending', 'confirmed', 'shipped', 'delivered'))
);
```


## Hands-on Practice (10-15 minutes)
**The Memory Test:** Close all notes. Create an entire mini database from scratch:

1. Choose a system (school, hospital, restaurant, gym — your choice)
2. Design 3 tables with at least 5 columns each
3. Use all these constraint types: PRIMARY KEY, NOT NULL, UNIQUE, DEFAULT, CHECK
4. Insert at least 3 rows per table
5. Run SELECT * to verify everything works

```sql
-- Write your own 3-table database here from memory!
-- Example theme: School Management System
-- Tables needed: students, courses, enrollments

-- YOUR CODE HERE (do not look at examples above!)
```

**Self-check questions after you finish:**
- Did every table have a PRIMARY KEY?
- Did you use NOT NULL on required fields?
- Did you use DEFAULT where it makes sense?
- Did you use CHECK for any validation?
- Did you use UNIQUE where appropriate?
## Technical Vocabulary

| Term | Meaning | Khmer |
|------|---------|-------|
| Review | Looking back at what you learned | ពិនិត្យឡើងវិញ |
| Checkpoint | A test point to verify understanding | ចំណុចពិនិត្យ |
| From memory | Without looking at notes or examples | ពីការចាំ |
| Mini database | Small but complete database system | មូលដ្ឋានទិន្នន័យតូច |
| Self-assessment | Evaluating your own understanding | ការវាយតម្លៃខ្លួនឯង |


## Speaking Practice
"This week I learned how to create professional database tables. I can define columns with appropriate data types and protect data quality using constraints. NOT NULL ensures required fields are never empty. UNIQUE prevents duplicates. CHECK validates data against custom rules. DEFAULT provides automatic values. I can now design a complete multi-table database from scratch."

## Reading Practice
Review your own CREATE TABLE statements from Days 10-13. For each table:
1. Count how many constraints you used
2. Identify if any columns are missing constraints they should have
3. Check if your data types are appropriate for each column
4. Read the SQL as if explaining it to a new team member

## Review Task
**Week 2 Self-Assessment Checklist:**
- [ ] I can write CREATE TABLE from memory
- [ ] I know all common data types and when to use each
- [ ] I understand PRIMARY KEY and AUTOINCREMENT
- [ ] I can use NOT NULL correctly
- [ ] I can use UNIQUE correctly
- [ ] I can write CHECK constraints
- [ ] I can set DEFAULT values
- [ ] I can design a 3-table database for a real scenario
- [ ] I can explain constraints to a non-technical person
- [ ] I can insert data and predict which INSERTs will fail

**If you cannot check all boxes, review Days 8-13 before continuing.**

## Expected Result for Today
You can create a complete mini database from memory without any reference. You understand all constraint types and can combine them effectively. You are ready to move forward to Week 3 topics (INSERT, UPDATE, DELETE operations).