# Day 178: Final Exam Simulation - Part 3 (Database Design)

## Section 1: Topic
**Final Exam - Database Design (Design a Complete 5-Table System from Requirements)**

## Section 2: English Explanation
This exam tests your database design skills. Given a business scenario, you must design a complete system with 5 tables, define relationships, set primary and foreign keys, apply constraints, and normalize to 3NF. This represents a real-world design task.

## Section 3: Khmer Explanation
ការប្រឡងនេះធ្វើតេស្តជំនាញរចនាមូលដ្ឋានទិន្នន័យ។ ពីសេណារីយ៉ូអាជីវកម្ម អ្នកត្រូវរចនាប្រព័ន្ធពេញលេញមាន ៥ តារាង កំណត់ទំនាក់ទំនង keys និង normalize ទៅ 3NF។

## Section 4: Design Scenario

**Scenario: Online Bookstore System**
Design a database for an online bookstore with these requirements:
- Store book information (title, ISBN, price, publication year, page count)
- Track authors (name, biography, nationality) — a book can have multiple authors
- Manage categories (fiction, science, history, etc.) — a book belongs to one category
- Record customers (name, email, address, phone, registration date)
- Track orders (order date, total amount, status) with order items (quantity, unit price)

**Your tasks:**
1. Identify all 5 tables with columns
2. Define primary keys and foreign keys
3. Add appropriate constraints (NOT NULL, UNIQUE, CHECK)
4. Draw relationships (one-to-many, many-to-many)
5. Ensure the design is in 3NF

## Section 5: Model Answer

```sql
-- Table 1: categories
CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL UNIQUE,
    description TEXT
);


-- Table 2: authors
CREATE TABLE authors (
    id SERIAL PRIMARY KEY,
    name VARCHAR(150) NOT NULL,
    biography TEXT,
    nationality VARCHAR(80)
);

-- Table 3: books
CREATE TABLE books (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    isbn VARCHAR(13) UNIQUE NOT NULL,
    price DECIMAL(10,2) CHECK (price > 0),
    publication_year INT CHECK (publication_year > 1900),
    page_count INT CHECK (page_count > 0),
    category_id INT NOT NULL REFERENCES categories(id)
);

-- Table 4: book_authors (many-to-many junction)
CREATE TABLE book_authors (
    book_id INT REFERENCES books(id) ON DELETE CASCADE,
    author_id INT REFERENCES authors(id) ON DELETE CASCADE,
    PRIMARY KEY (book_id, author_id)
);

-- Table 5: customers
CREATE TABLE customers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(150) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    address TEXT,
    phone VARCHAR(20),
    registration_date DATE DEFAULT CURRENT_DATE
);

-- Table 6: orders
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    customer_id INT NOT NULL REFERENCES customers(id),
    order_date TIMESTAMP DEFAULT NOW(),
    total_amount DECIMAL(12,2) CHECK (total_amount >= 0),
    status VARCHAR(20) DEFAULT 'pending' CHECK (status IN ('pending','shipped','delivered','cancelled'))
);

-- Table 7: order_items
CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    order_id INT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
    book_id INT NOT NULL REFERENCES books(id),
    quantity INT NOT NULL CHECK (quantity > 0),
    unit_price DECIMAL(10,2) NOT NULL CHECK (unit_price > 0)
);
```


## Section 6: Relationship Summary
| Relationship | Type | Implementation |
|---|---|---|
| categories → books | One-to-Many | category_id FK in books |
| authors ↔ books | Many-to-Many | book_authors junction table |
| customers → orders | One-to-Many | customer_id FK in orders |
| orders → order_items | One-to-Many | order_id FK in order_items |
| books → order_items | One-to-Many | book_id FK in order_items |

## Section 7: Technical Vocabulary
| Term | Meaning |
|------|---------|
| Junction table | តារាងភ្ជាប់ (many-to-many) |
| 3NF compliance | ការអនុលោម 3NF |
| Design scenario | សេណារីយ៉ូរចនា |

## Section 8: Speaking Practice
Explain to a client: "I designed a normalized database with proper relationships, constraints, and referential integrity for your bookstore system."

## Section 9: Review Task
Compare your design attempt with the model answer. Check: Did you identify the many-to-many relationship? Did you add CHECK constraints? Did you normalize properly?

## Section 10: Expected Result
You can design a complete multi-table database from business requirements with proper keys, constraints, and normalization.
