# Day 92: Subqueries in WHERE with IN

## Topic
Using subqueries with the IN operator to filter rows based on a list of values returned by another query.

## English Explanation
When a subquery returns multiple values (a list), you use the **IN** operator instead of = in the WHERE clause. The subquery produces a set of values, and the outer query checks if each row's value exists in that set. This is powerful for finding rows that belong to a specific group defined by another table.

## Khmer Explanation (ការពន្យល់ជាភាសាខ្មែរ)
នៅពេលដែល subquery ត្រឡប់តម្លៃច្រើន (ជាបញ្ជី) យើងប្រើ operator **IN** ជំនួសឱ្យ = នៅក្នុង WHERE clause។ Subquery បង្កើតសំណុំតម្លៃ ហើយ outer query ពិនិត្យមើលថាតើតម្លៃនៃជួរដេកនីមួយៗមានក្នុងសំណុំនោះឬអត់។ នេះមានប្រយោជន៍ខ្លាំងសម្រាប់ស្វែងរកជួរដេកដែលស្ថិតក្នុងក្រុមជាក់លាក់មួយ។

## SQL Example

```sql
-- Find employees in departments located in Phnom Penh
SELECT first_name, last_name, department_id
FROM employees
WHERE department_id IN (
    SELECT id FROM departments WHERE location = 'Phnom Penh'
);

-- Find customers who have placed orders
SELECT customer_name, email
FROM customers
WHERE customer_id IN (
    SELECT DISTINCT customer_id FROM orders
);

-- Find products that have never been ordered
SELECT product_name, price
FROM products
WHERE product_id NOT IN (
    SELECT DISTINCT product_id FROM order_items
);
```

## Hands-on Practice (10-15 minutes)

**Task 1:** Find all employees who work in departments with more than 10 people.

```sql
SELECT first_name, last_name, department_id
FROM employees
WHERE department_id IN (
    SELECT department_id
    FROM employees
    GROUP BY department_id
    HAVING COUNT(*) > 10
);
```


**Task 2:** Find customers who ordered a specific product (product_id = 5).

```sql
SELECT customer_name, phone
FROM customers
WHERE customer_id IN (
    SELECT customer_id
    FROM orders o
    JOIN order_items oi ON o.order_id = oi.order_id
    WHERE oi.product_id = 5
);
```

## Technical Vocabulary

| Term | Definition | Khmer |
|------|-----------|-------|
| IN operator | Checks if a value exists in a list | ពិនិត្យថាតម្លៃមានក្នុងបញ្ជីឬអត់ |
| NOT IN | Checks if a value does NOT exist in a list | ពិនិត្យថាតម្លៃមិនមានក្នុងបញ្ជី |
| Multi-value subquery | A subquery returning multiple rows | Subquery ត្រឡប់ជួរដេកច្រើន |
| Set | A collection of distinct values | សំណុំតម្លៃមិនស្ទួន |
| Filter | Narrowing results based on conditions | ច្រោះលទ្ធផលតាមលក្ខខណ្ឌ |

## Speaking Practice
"We use IN with a subquery when we need to check if a value matches any item in a list. For example, to find customers in cities where we have offices, we first query our office locations, then use IN to match customer cities against that list."

## Reading Practice
Read and explain:
```sql
SELECT product_name FROM products
WHERE category_id IN (SELECT id FROM categories WHERE active = TRUE);
```
Answer: It selects product names from products whose category is currently active (active = TRUE in categories table).

## Review Task
1. When do you use IN instead of = with a subquery?
2. What does NOT IN do?
3. Write a query to find employees NOT in the 'Sales' department using a subquery.
4. Can a subquery with IN return NULL values? What happens?

## Expected Result
By the end of today, you can use subqueries with IN and NOT IN to filter rows based on lists generated by another query, connecting data across multiple tables without using JOIN.
